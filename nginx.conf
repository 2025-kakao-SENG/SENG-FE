server {
    listen       80;
    server_name  localhost;

    # 정적 파일이 위치한 디렉토리 (Dockerfile에서 복사한 경로)
    root /usr/share/nginx/html;
    index index.html;

    # 모든 요청을 처리 (SPA 라우팅 대응)
    location / {
        try_files $uri $uri/ /index.html;
    }
}